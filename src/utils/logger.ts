export type AddJobToQueueLabelType = {
    operation: string,
    subOperation: string,
    type: string,
};

export type RegisterWorkerLabelType = {
    operation: string,
    subOperation: string,
    type: string,
}